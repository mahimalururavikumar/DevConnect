spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devconnect
    username: root
    password: 6301
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080